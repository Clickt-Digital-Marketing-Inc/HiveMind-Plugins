---
description: Onboard a new ecomm client into Clickt's hosted reporting (scaffold engine, configure sources, hosting + auth, schedule weekly Routine)
argument-hint: [client repo path]
---

Use the clickt-reporting:client-report-setup skill to onboard a client into the hosted
reporting system. Client repo path (if given): $ARGUMENTS — otherwise ask. Follow the
skill exactly: interview → scaffold from the plugin's bundled template → configure →
verify sources → hosting with per-client basic auth → schedule the Monday-8am weekly
Routine (confirm timing).
